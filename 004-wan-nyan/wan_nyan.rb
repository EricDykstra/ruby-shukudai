class WanNyan
  class << self

    def run(n)
      # array 1 から n 作って
      # 以下の優先順でどれか一つを出力する
      # 15 で割り切れる場合は WanNyan を出力する
      # 4 で割り切れる場合は Nyan を出力する
      # 3 で割り切れる場合は Wan を出力する
      # その他の場合は入力した数値をそのまま出力する
      # 全部はstringにする
      # ["1", "2", "Wan", "Nyan"...]
    end

  end
end
